// Phase 7 — CDC + SCD Type 2 simulator (ADF-1030). A build sim (same shape as the
// medallion sims), enriched with evidence panels (incoming change records).

export const cdcSim = {
  id: 'adf-1030-cdc',
  title: 'Customer Master CDC Pipeline',
  ticketTitle: 'Customer Master CDC Pipeline',
  subtitle: 'Track customer profile changes over time with CDC, incremental loads, Delta MERGE, and SCD Type 2 history.',
  businessOwner: 'Nadia Khan · Customer Analytics Lead',
  due: 'End of sprint',
  ticket: {
    id: 'ADF-1030', owner: 'Nadia Khan', created: 'Today 9:40 AM', due: 'End of sprint',
    priority: 'P1', environment: 'Production', businessTeam: 'Customer Analytics', status: 'Assigned',
    estimatedEffort: '3 hours', difficulty: 'Advanced',
    skills: ['CDC', 'Incremental loads', 'SCD Type 2', 'Delta MERGE', 'Dimensional modeling'],
    acceptanceCriteria: [
      { label: 'Incremental load configured', step: 0 },
      { label: 'CDC method selected', step: 1 },
      { label: 'Changes routed by operation', step: 2 },
      { label: 'SCD Type 2 dimension designed', step: 3 },
      { label: 'MERGE expires old & inserts new', step: 4 },
      { label: 'History preserved & validated', step: 5 },
    ],
    definitionOfDone: ['Pipeline deployed', 'CDC enabled on source', 'SCD2 dimension registered', 'MERGE validated', 'Documentation updated', 'Analytics lead signoff'],
    comments: [
      { author: 'Nadia Khan', role: 'Customer Analytics Lead', text: 'Marketing wants to see how a customer’s loyalty tier and address changed over time — not just the latest value.' },
      { author: 'Data Platform', role: 'Platform Engineering', text: 'Source is Azure SQL. Please use change data capture, not nightly full reloads.' },
    ],
    attachments: [{ name: 'Customer Dimension Spec.md', type: 'MD' }, { name: 'CDC Source Schema.json', type: 'JSON' }],
    activity: [{ time: '09:40 AM', text: 'Ticket created' }, { time: '09:42 AM', text: 'Assigned to you' }],
    closed: { completedTime: '4:25 PM', ownerApproved: 'Yes', businessImpact: 'Full customer change history available for analysis.' },
  },
  businessImpact: {
    need: 'Marketing wants historical visibility into customer address, email, and loyalty-tier changes.',
    without: ['No history of customer changes — only the latest value.', 'Cannot analyze loyalty-tier movement over time.', 'Overwrites silently destroy past states.'],
  },
  interviewQuestions: [
    'What is CDC (Change Data Capture)?',
    'What is SCD Type 2?',
    'What is the difference between SCD Type 1 and SCD Type 2?',
    'How do you implement an SCD2 MERGE?',
    'How do you handle deletes in CDC?',
  ],
  pipelineNodes: [
    { label: 'Ticket', build: null }, { label: 'Load', build: 0 }, { label: 'CDC', build: 1 },
    { label: 'Changes', build: 2 }, { label: 'SCD2', build: 3 }, { label: 'MERGE', build: 4 }, { label: 'Validate', build: 5 },
  ],
  validationRules: [
    { key: 'c0', label: 'Load strategy', stepIndex: 0 },
    { key: 'c1', label: 'CDC method', stepIndex: 1 },
    { key: 'c2', label: 'Change routing', stepIndex: 2 },
    { key: 'c3', label: 'SCD2 design', stepIndex: 3 },
    { key: 'c4', label: 'MERGE logic', stepIndex: 4 },
    { key: 'c5', label: 'History validated', stepIndex: 5 },
  ],
  readinessDims: [
    { key: 'CDC design', steps: [0, 1] },
    { key: 'History tracking', steps: [3, 5] },
    { key: 'MERGE logic', steps: [4] },
    { key: 'Data quality', steps: [2, 5] },
  ],
  summary: [
    { label: 'Load', step: 0, field: 'decision' }, { label: 'CDC', step: 1, field: 'decision' },
    { label: 'Design', step: 3, field: 'design' }, { label: 'Key', step: 3, field: 'key' },
    { label: 'MERGE', step: 4, field: 'whenMatched' },
  ],
  warnings: [
    'Load strategy not chosen yet.',
    'CDC method not selected yet.',
    'Change operations are not routed yet.',
    'SCD Type 2 dimension is not designed yet.',
    'MERGE logic is not configured yet.',
    'Historical tracking is not validated yet.',
  ],
  review: {
    strengths: ['Incremental load chosen over full reload', 'CDC captures inserts, updates, and deletes', 'SCD Type 2 preserves full history', 'MERGE expires the old version and inserts the new'],
  },
  experienceDelta: { cdc: 1, sqlPipelines: 1 },
  briefing: {
    projectGoal: 'Build a CDC-driven customer dimension with SCD Type 2 history.',
    architecture: ['Azure SQL + CDC', 'Incremental load', 'Classify changes', 'SCD2 dimension', 'Delta MERGE', 'History validated'],
    estimatedTime: '20 minutes', difficulty: 'Advanced',
  },
  steps: [
    {
      id: 'load-strategy', title: 'Load Strategy', configPanelTitle: 'Load Strategy',
      theory: {
        what: 'The load strategy decides how much data moves each run — everything, or only what changed.',
        why: 'The customer table is large and changes slowly; reloading it all every run is wasteful and slow.',
        interview: '"Why incremental over full load?" — it is cheaper, faster, and only processes changed rows.',
      },
      fields: [{ id: 'decision', label: 'How should the customer master load?', type: 'radio', options: ['Full Load', 'Incremental Load', 'Manual Upload', 'Snapshot Export'], default: '', correct: 'Incremental Load', hint: 'Only move the rows that changed.' }],
      azurePath: ['ADF Studio', 'Author', 'Pipeline', 'Load strategy'],
      correctBecause: 'The table is large and slowly changing, so an incremental load only processes changed rows.',
      avoid: { option: 'Full Load', reason: 'Reloading the whole customer table every run is slow and expensive.' },
      mentor: 'Choose the strategy that only moves changed rows.',
      commonMistake: 'Full reloads on a large, slowly-changing table.',
      expectedResult: 'The pipeline loads only the customer rows that changed.',
      successTitle: 'Incremental load configured',
      takeaway: 'For slowly-changing master data, use incremental loads, not full reloads.',
      impacts: { decision: 'A full reload reprocesses the entire customer table every run and inflates cost and runtime.' },
    },
    {
      id: 'cdc-method', title: 'CDC Method', configPanelTitle: 'Change Capture Method',
      theory: {
        what: 'Change Data Capture (CDC) records every insert, update, and delete on the source table.',
        why: 'To build history you need not just which rows changed, but what changed — including deletes.',
        interview: '"CDC vs Change Tracking?" — CDC captures the actual changed data (and deletes); Change Tracking only flags which rows changed.',
      },
      fields: [{ id: 'decision', label: 'How will you capture source changes?', type: 'radio', options: ['SQL Change Tracking', 'CDC', 'Timestamp Watermark', 'Full Refresh'], default: '', correct: 'CDC', hint: 'You need inserts, updates, AND deletes with the changed values.' }],
      azurePath: ['Azure SQL', 'Enable CDC', 'cdc.dbo_Customer_CT'],
      correctBecause: 'CDC captures inserts, updates, and deletes with column-level change data — exactly what SCD2 history needs.',
      avoid: { option: 'Timestamp Watermark', reason: 'A timestamp watermark misses deletes and any change that doesn’t update the timestamp.' },
      mentor: 'Pick the method that captures inserts, updates, and deletes with their values.',
      commonMistake: 'Using a timestamp watermark (misses deletes) or full refresh (no incrementality).',
      expectedResult: 'CDC captures every insert, update, and delete from the source.',
      successTitle: 'CDC method selected',
      takeaway: 'CDC captures change data (incl. deletes); Change Tracking only flags changed rows.',
      impacts: { decision: 'A timestamp watermark silently misses deletes, leaving stale customers in the dimension.' },
    },
    {
      id: 'classify', title: 'Classify Changes', configPanelTitle: 'Route Source Changes',
      theory: {
        what: 'Each CDC row carries an operation: insert, update, or delete.',
        interview: '"CDC event types?" — I (insert), U (update / before+after), D (delete) — route each to the right MERGE branch.',
      },
      evidence: { type: 'records', title: 'Incoming CDC batch', columns: ['CustomerID', 'Name', 'Tier', 'UpdatedDate', 'Op'], rows: [
        ['C001', 'Ada Lovelace', 'Gold', '2026-06-01', 'U'],
        ['C014', 'Alan Turing', 'Silver', '2026-06-01', 'I'],
        ['C009', 'Grace Hopper', '—', '2026-06-01', 'D'],
      ] },
      fields: [{ id: 'decision', label: 'How should these CDC operations be handled?', type: 'radio', options: ['Apply everything as inserts', 'Route by operation (I / U / D) to MERGE', 'Ignore deletes', 'Overwrite the whole table'], default: '', correct: 'Route by operation (I / U / D) to MERGE', hint: 'Inserts, updates, and deletes each need different MERGE behavior.' }],
      azurePath: ['ADF Studio', 'Author', 'Data Flow', 'Conditional split (by __$operation)'],
      correctBecause: 'Inserts, updates, and deletes each map to a different MERGE branch — routing by operation keeps the dimension correct.',
      avoid: { option: 'Ignore deletes', reason: 'Ignoring deletes leaves removed customers active in the dimension forever.' },
      mentor: 'Split the CDC feed by operation and send each to the right MERGE branch.',
      commonMistake: 'Treating every change as an insert, or dropping deletes.',
      expectedResult: 'CDC rows are routed by operation (insert / update / delete) into the MERGE.',
      successTitle: 'Changes routed',
      takeaway: 'Route CDC by operation type — inserts, updates, and deletes behave differently.',
      impacts: { decision: 'Applying everything as inserts duplicates customers; ignoring deletes leaves stale active rows.' },
    },
    {
      id: 'scd2', title: 'SCD Type 2 Design', configPanelTitle: 'Dimension Design',
      theory: {
        what: 'SCD Type 2 keeps a new row for each version of a customer, with effective/expiry dates and a current flag.',
        interview: '"SCD1 vs SCD2?" — Type 1 overwrites (no history); Type 2 adds a new versioned row and preserves history.',
      },
      evidence: { type: 'facts', title: 'Target dimension: DimCustomer', items: [
        { label: 'CustomerKey', value: 'Surrogate key (per version)' },
        { label: 'CustomerID', value: 'Business key' },
        { label: 'Tier / Address / Email', value: 'Tracked attributes' },
        { label: 'EffectiveDate / ExpiryDate', value: 'Version validity window' },
        { label: 'CurrentFlag', value: '1 = current, 0 = historical' },
      ] },
      fields: [
        { id: 'design', label: 'Dimension design', type: 'radio', options: ['SCD Type 2 (history)', 'SCD Type 1 (overwrite)', 'Snapshot (no history)'], default: '', correct: 'SCD Type 2 (history)', hint: 'Marketing needs history, not just the latest value.' },
        { id: 'key', label: 'Primary key', type: 'radio', options: ['Surrogate CustomerKey', 'Natural CustomerID'], default: '', correct: 'Surrogate CustomerKey', hint: 'Each version needs its own key.' },
      ],
      azurePath: ['ADF Studio', 'Author', 'Datasets', 'DimCustomer (Delta)'],
      correctBecause: 'SCD Type 2 with a surrogate key per version preserves the full history of each customer attribute.',
      avoid: { option: 'SCD Type 1 (overwrite)', reason: 'Type 1 overwrites the row, destroying the history Marketing needs.' },
      mentor: 'Use SCD Type 2 with a surrogate key, effective/expiry dates, and a current flag.',
      commonMistake: 'Using SCD Type 1 (overwrite) or the natural key as the primary key.',
      expectedResult: 'A SCD Type 2 dimension is designed with a surrogate key and validity columns.',
      successTitle: 'SCD Type 2 designed',
      takeaway: 'SCD2 = surrogate key per version + effective/expiry dates + current flag.',
      impacts: { design: 'SCD Type 1 overwrites the row and loses all history.', key: 'A natural key cannot hold multiple versions of the same customer.' },
    },
    {
      id: 'merge', title: 'MERGE Logic', configPanelTitle: 'Delta MERGE Configuration',
      theory: {
        what: 'A Delta MERGE applies the changes: expire the old version and insert the new one.',
        interview: '"How do you implement an SCD2 MERGE?" — WHEN MATCHED and attribute changed → expire old; insert the new current row.',
      },
      fields: [
        { id: 'whenMatched', label: 'WHEN MATCHED (key exists, attribute changed)', type: 'radio', options: ['Expire old row (ExpiryDate set, CurrentFlag = 0)', 'Update the row in place', 'Delete the row'], default: '', correct: 'Expire old row (ExpiryDate set, CurrentFlag = 0)', hint: 'Close out the previous version — do not overwrite it.' },
        { id: 'whenNotMatched', label: 'WHEN NOT MATCHED (new customer or new version)', type: 'radio', options: ['Insert new version (CurrentFlag = 1)', 'Ignore', 'Update existing'], default: '', correct: 'Insert new version (CurrentFlag = 1)', hint: 'Add the new current row.' },
      ],
      azurePath: ['ADF Studio', 'Author', 'Data Flow', 'Alter Row + Sink (MERGE)'],
      correctBecause: 'Expiring the matched old row and inserting a new current row is exactly the SCD Type 2 MERGE pattern.',
      avoid: { option: 'Update the row in place', reason: 'Updating in place is SCD Type 1 — it overwrites and loses history.' },
      mentor: 'On match, expire the old row; on no-match, insert the new current version.',
      commonMistake: 'Updating in place (SCD1) instead of expiring + inserting.',
      expectedResult: 'The MERGE expires the old version and inserts a new current row.',
      successTitle: 'MERGE logic configured',
      takeaway: 'SCD2 MERGE: expire the matched old row, insert a new current row.',
      impacts: { whenMatched: 'Updating in place overwrites history — that is SCD Type 1, not Type 2.', whenNotMatched: 'Ignoring new versions means changes never get recorded.' },
    },
    {
      id: 'validate', title: 'Validate History', configPanelTitle: 'Validate Historical Tracking',
      theory: {
        what: 'Validation confirms a tier change produced a new versioned row and expired the old one.',
        interview: '"How do you handle deletes?" — soft-expire the row (ExpiryDate + CurrentFlag = 0); never hard-delete history.',
      },
      evidence: { type: 'validation', title: 'Customer C001 moved Silver → Gold → Platinum', items: [
        { label: 'Gold row', value: 'Expired — ExpiryDate set, CurrentFlag = 0', ok: true },
        { label: 'Platinum row', value: 'Created — CurrentFlag = 1', ok: true },
        { label: 'History query', value: 'Returns the full tier timeline', ok: true },
      ] },
      fields: [{ id: 'decision', label: 'History looks correct. What now?', type: 'radio', options: ['Confirm history preserved & close', 'Reopen', 'Overwrite to latest tier only'], default: '', correct: 'Confirm history preserved & close', hint: 'Old row expired, new row current, full timeline queryable.' }],
      azurePath: ['ADF Studio', 'Monitor', 'Validate DimCustomer history'],
      correctBecause: 'The old version is expired, the new version is current, and the full tier history is queryable — SCD2 is working.',
      avoid: { option: 'Overwrite to latest tier only', reason: 'Overwriting to the latest tier destroys the history you just built.' },
      mentor: 'Confirm the old row expired and the new row is current before closing.',
      commonMistake: 'Closing without checking history, or overwriting to the latest value.',
      expectedResult: 'You confirm the old row expired, the new row is current, and history is queryable.',
      successTitle: 'Historical tracking validated',
      takeaway: 'Validate SCD2 by querying the full version history before closing.',
      impacts: { decision: 'Overwriting to the latest tier collapses the dimension back to Type 1 and loses history.' },
    },
  ],
  completion: {
    headline: 'CDC Pipeline Ready ✅',
    architectureSummary: 'Azure SQL CDC feeds an incremental load that routes inserts, updates, and deletes through a Delta MERGE into an SCD Type 2 customer dimension, preserving the full history of every customer attribute change.',
    architectureFlow: ['Azure SQL CDC', 'Incremental load', 'Route by op', 'SCD2 MERGE', 'DimCustomer'],
    finalArchitecture: ['Azure SQL + CDC', 'Incremental load', 'Classify (I / U / D)', 'SCD Type 2 design', 'Delta MERGE (expire + insert)', 'DimCustomer (history)'],
    productionNotes: ['CDC captures inserts, updates, and deletes.', 'MERGE expires the old version and inserts a new current row.', 'Deletes are soft-expired, never hard-deleted.'],
    interviewLabel: 'Interview answer · CDC & SCD Type 2',
    interviewAnswer: '"I built a CDC-driven customer dimension. Azure SQL CDC captures inserts, updates, and deletes; an incremental load brings only changed rows; a conditional split routes each operation; and a Delta MERGE applies SCD Type 2 — expiring the old version (ExpiryDate, CurrentFlag = 0) and inserting a new current row — so the full history of address, email, and tier changes is preserved and queryable."',
    resumeBullet: 'Implemented a CDC-driven customer dimension pipeline using incremental loads, Delta MERGE operations, and SCD Type 2 logic to preserve historical customer attribute changes.',
  },
};
